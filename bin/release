#!/bin/sh

BUILD_DIR=$1

echo "BUILD DIR of release is\n"
echo "$BUILD_DIR"

cat <<EOF
---
config_vars:
  JAVA_OPTS: -Xmx384m -Xss512k -XX:+UseCompressedOops
default_process_types:
  web: bash ${BUILD_DIR}/setupSonar.sh
EOF
